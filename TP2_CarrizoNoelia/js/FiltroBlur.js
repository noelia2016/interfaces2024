class FiltroBlur extends Filtro {

    constructor(context, whidth, height) {
        this.ctx = context;
        this.height = height;
        this.whidth = whidth;
    } // constructor

    // funcion para aplicar el filtro deseado dependiendo de la opcion que elija el usuario
    aplicarFiltro (imageData){

       
    } // aplicarFiltro

} // fin de class del filtro blur